let myPlugins = {
    install(Vue, options){
        // Vue.prototype.$bus
        // Vue.directive
        // Vue.component
        // Vue.filter
        // Vue.directive(options.name,(element,{value})=>{
        //     setTimeout(() => {
        //         element.innerHTML = value.toUpperCase()
        //     }, 3000);
            
        // })
    }
}
export default myPlugins
